#include <stdio.h>

int main()
{
    int a[50],n;
    printf("Enter the size: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++){
        printf("%d",a[i]);
    }
    return 0;
}
//just input and printing the output.
